package com.example.bulletinboard;

import org.junit.Test;

import java.util.ArrayList;

/**
 * Created by alexandre on 06/11/17.
 */

public class postsTest {

    @Test
    public void getPostsTest(){
        /*
        Posts p = Posts.getInstance();

        Post post1 = new Post("Title1", "Description1", true, true);
        Post post2 = new Post("Title2", "Description2", true, true, "02-OCT-2017");
        Post post3 = new Post("Title3", "Description3");

        p.addPost(post1);
        p.addPost(post2);
        p.addPost(post3);

        ArrayList<Post> postList = new ArrayList<>();
        postList = p.getPosts();
        assert(postList.contains(post1));

        assert post1.getTitle().equals("Title1");
        assert post2.getShowEmail() == true;
        assert post3.getShowPhone() == false;
        assert post1.getDescription().equals("Description1");*/
    }
}
